<div class="mt-3">
  <%= t('projects.tasks_total') %>:
  <%= @current_project.tasks.count %>
  <%= t('projects.minutes_total') %>:
  <%= @current_project.project_time_spent %>
  <% unless @current_project.salary_per_hour.nil? %>
    <%= t('projects.salary_total') %>:
    <%= @current_project.project_salary %>
  <% end %>
</div>